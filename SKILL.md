---
name: chem-safety-design-review
description: 化工建设项目"安全设施设计专篇"的专家级评审诊断技能。当用户要求对安全设施设计专篇（含中试专篇）做诊断、审查、校稿、专家意见落实核查、多版本修改对比、上会或报批通过性评估时使用；也适用于复核企业对专家意见的修改回复。对标政府聘请专家的上会审查水平，主线是"章节完整性、危险辨识与设施映射、标准符合性、数据闭环、计算复核、意见闭环"，输出分 A 类、B 类、C 类的缺陷清单与通过性结论。检索标准依据时优先走 WeKnora 内网规范库，断网自动降级到本地规范库全文检索，再降级到内置锚点卡。
---

# 安全设施设计专篇评审诊断

本技能对标政府专家的上会审查水平，给安全设施设计专篇做诊断。

先说清楚立场。只核对程序、文本、编辑格式，那是秘书级的校稿，不是专家审查。专家级审查要做到工程师的深度，具体包含六件事：

1. **章节完整性**——该有的章节和内容有没有齐全。
2. **条款级适用性**——引用的标准是不是这个行业该用的（精细化工不能套石油化工的标准）。
3. **数据闭环核验**——同一个数在不同章节、不同表、不同附件里是否一致。
4. **计算复核**——报告给出的计算，要独立重算一遍。
5. **危险辨识与设施映射**——每一项辨识出来的危险，是否都配了对应的安全设施，并落到具体设备位号和参数。
6. **工艺纵深**——顺着工艺流程看安全设施设置得合不合理。

诊断绝不能停留在前面的秘书级校稿。

> 本技能只审"安全设施设计专篇"这一个文种。安评、能评等其他文种不在范围内。但要注意：专篇经常照抄安评的附表，安评里的辨识错误会一位一位地沿传到专篇里。所以辨识类的计算必须自己独立重算，不能因为"上游文种已经核过了"就放行。

## 〇、证据分层与 WeKnora 兼容协议（先于一切工序）

标准依据从哪里查，本技能用三层证据架构。每次诊断开工前先探测哪一层可用，并在诊断报告的概况章里声明"本次证据等级"。

### 三层的定义与探测顺序

| 层级 | 证据来源 | 什么时候用 | 证据等级 |
|---|---|---|---|
| **L1** | WeKnora 内网规范库（向量加关键词的混合检索、图谱检索） | 凭证配好了、接口能连通 | 最高 |
| **L2** | 本地规范库全文检索（对同一批标准的 md 文件做全文搜索） | 只有当 L1 完全用不了（断网或没凭证）时才启用 | 高 |
| **L3** | 本技能内置的锚点卡 [references/standard-anchors.md](references/standard-anchors.md) | L1、L2 都用不了 | 兜底（数值一律标 `[需复核]`） |

**探测怎么做**：探测和三层检索都封装进了可复用脚本 [scripts/weknora_probe.sh](scripts/weknora_probe.sh)，直接 source 调用即可，不用每次照抄命令。

```bash
source scripts/weknora_probe.sh   # 载入函数，并自动加载配置
chem_probe                        # 探测，打印证据等级（L1/L2/L3）并导出
chem_search "GBT50493 探测器 水平距离"   # 按当前证据等级自动检索
```

**注意两件事**：

- 上面三条命令必须在**同一个 shell 会话**里连续执行。因为 `EVIDENCE_LEVEL` 是导出到当前会话的，分开跑就丢了。为防误判，`chem_search` 在没探测过时会自动先跑一次探测。
- **凭证放在配置文件里，不要指望 `~/.bashrc`**。本技能跑命令用的是非交互 shell，它不读 `~/.bashrc`。脚本会主动去读配置文件，查找顺序是：环境变量 `CHEM_SAFETY_ENV` 指向的文件 → `~/.claude/chem-safety.env` → 技能目录随附的 `chem-safety.env`。把下面四个变量写进 `~/.claude/chem-safety.env` 即可：

```bash
export WEKNORA_BASE_URL="http://主机:端口/api/v1"
export WEKNORA_API_KEY="sk-你的密钥"
export WEKNORA_KB_IDS="知识库id1,知识库id2"
export CHEM_STD_LIB="本地规范库 documents 目录的路径"
```

脚本内部的判断逻辑：先看 `WEKNORA_BASE_URL` 和 `WEKNORA_API_KEY` 配了没、接口连不连得通，通了就是 L1；不通再看 `CHEM_STD_LIB` 目录在不在，在就是 L2；都不行就 L3。L1 情况下如果没设 `WEKNORA_KB_IDS`，脚本会自动列出可用知识库让你选。L3 情况下会打印待复核警告。

检索用的三个函数 `wk_api`、`wk_search`、`wk_hybrid` 都在脚本里，本技能直接用它们连 WeKnora 接口检索，不需要另外安装独立的 weknora 技能。（如果环境里恰好装了 weknora 技能，也可以改用它，但那不是必需的。）

### 几条关键规则

- **L1 和 L2 不同时跑**：L1 能用的时候就不跑本地全文检索，避免重复劳动；L2 只在断网时兜底。
- **引用必须给出处**。凡是引用了标准的数值或条款，都要能追溯到来源：L1 给"知识库标题加 chunk 序号"，L2 给"文件路径加行号或章节号"，L3 标"需复核，来源是内置锚点卡"。
- **L3 情况下必须强制声明复核**。证据等级是 L3 时，诊断报告的结论章里要显著写明：本次没接规范库，所有标准数值都来自内置锚点卡，上会前必须逐条对照现行标准原文复核。

## 一、强制执行的环境要求（防止诊断发散）

> 实战教训：同一个技能多次执行，常因为工具链版本有代差、知识库输入不一样、否决项的标尺没提前声明，导致每次查出的问题数量和最终结论差异很大。所以下面这些从"最佳实践"升级成了**强制工序**，任何专篇诊断都要先满足这些，再开工。

- **M1 全文落盘、可检索（强制）**：受审的专篇必须先用 pandoc 之类的工具转成纯文本落到本地，表格要全量导出、图片要提取出来。整个诊断过程都拿这份落盘文本做全文检索比对，不能只靠上下文窗口读个印象就下结论。表格不许抽查，必须全量导出逐项比对。图片形式的反应方程式、联锁逻辑图要提取出来（WMF 等格式先转码）用眼睛核。
- **M2 计算复核要留痕、建台账（强制）**：凡是要复算的，都用能执行的计算工具算（不许心算），并且留痕：复核的是哪项、报告原来的口径、输入值、用的公式、重算结果、结论，逐项填进"关键计算独立复核表"。物料平衡表要整表复算，储量要按几何上限验证。
- **M3 法规时效要当天搜索复核（强制）**：诊断当天，对专篇里引用的每一本标准、每一个文件文号，都要核一遍它现在还有没有效。L1 或 L2 能用时，以规范库清单里标的版本年和状态为准，并搜索复核；L3 时只能凭内置索引判断，必须显著标注"待复核"。绝不允许凭模型记忆直接下定论。产出"法规时效核查留痕表"。
- **M4 程序要件要逐项核查（强制）**：不管这次评审定位是什么，都要逐项核查并单列一节：安全条件审查意见书（要文号）、备案证和许可范围、治安防范设计（涉及剧毒或易制爆时，要核 GA 1511、GA 1002）、审查权限层级、重大危险源备案的衔接、附件和图纸目录的完整性以及实体是否真的存在。程序上的缺失和技术上的错误同等重要，都要列入否决评估。
- **M5 跨文种联动核对（有条件强制）**：只要能拿到上游的安评、环评、反应评估、危险与可操作性分析（HAZOP）等文件，就必须做跨文种联动核对——查错误有没有沿传、有没有模板残留、同一个设施在多个文件里说法矛不矛盾。拿不到这些文件时，在报告里说明没做这一步以及原因。
- **M6 评审标尺要事前声明（强制）**：在概况章里声明本次评审的标尺是哪一种（合规性评审、预审诊断、还是专家级诊断）、否决项的判定阈值（程序缺失算不算一票否决、B 类问题影不影响上会）、以及证据等级（L1/L2/L3）。最终结论的严格程度要和查出的 A 类问题严重程度自洽——如果查出了颠覆性错误却还给宽松结论，必须书面解释为什么。
- **M7 问题要做映射、控制颗粒度（强制）**：同一个根因引出的多个散点，合并成一条（把所有出现位置都标出来）；一个复合问题要拆成可以逐一核销的子项。多版本对比时必须输出"问题映射表"，不许用"问题总数差了几个"来代替逐条的映射分析。

## 二、专篇诊断十步工作流

每一步都标注了【输入 / 动作 / 产出 / 留痕】。第 4、6、7 步依赖 references 里的文件，按需加载。

1. **预处理落盘**【输入：专篇及附件原件｜动作：按 M1 用 pandoc 把全文、表格、图片都落盘｜产出：可检索的纯文本加资源清单｜留痕：落盘文件清单】
2. **文种定性与属地**【确认这确实是安全设施设计专篇、项目属于哪个省市区、是不是中试专篇、编制依据是哪个版本（AQ 3066-2025 还是 39 号导则）｜产出：项目画像卡】
3. **证据层探测**【执行〇章的探测协议｜产出：证据等级声明（L1/L2/L3）、可用的知识库 id 或本地库路径】
4. **章节完整性对照**【对照 [references/checklist-completeness.md](references/checklist-completeness.md) 逐章核必备内容｜产出：章节完整性表，每章判"齐备/缺项/不适用"三态】
5. **危险辨识复核加设施映射**【自己独立重算辨识类别和临界量，不采信安评；对每一项辨识出来的危险，核查有没有对应的安全设施、有没有落到具体设备位号和参数｜产出：危险-设施映射矩阵，每项危险对应到设施、落地位号和参数、以及缺失标记】
6. **逐类设施符合性**【按 [references/standard-anchors.md](references/standard-anchors.md) 的设施分类，检索取值（L1/L2/L3），逐类核选型和参数合不合标｜产出：设施符合性矩阵，判"通过/整改/否决"并附条款出处】
7. **可计算项重算**【按 [references/calc-cards.md](references/calc-cards.md) 复核事故水、消防水、气体检测布点、氮气与仪表风、储量、外部防护距离｜产出：关键计算独立复核表】
8. **数据闭环与图文一致**【把投料、产能、储量、负荷、临界量这些物理量在跨章节、跨表、跨附件之间比对，同一个量出现两次以上就必须一致；再核图和文里的关键标注（防爆分区半径、间距、位号）一不一致｜产出：数据一致性问题清单】
9. **意见三段闭环**（有历史意见时才做）【按 R1 到 R6 的规则，把每条意见拆成子项，对照企业的回复承诺，再到终稿里全文穿透核查｜产出：意见落实闭环核查表，判"已落实/部分落实/未闭合"】
10. **分级输出**【按 [references/output-templates.md](references/output-templates.md) 汇总，产出分 A/B/C 的缺陷清单，含通过性三档结论，最终生成单个 Word 文档｜见本文件"输出阶段"一节】

## 输出阶段：生成单个 Word 报告

诊断过程中，各步的表按 [references/output-templates.md](references/output-templates.md) 的模板边做边填。第 10 步汇总时，把这些数据填进一个结构化的 JSON 文件（字段对照 [scripts/report_schema.json](scripts/report_schema.json) 这个示例），然后运行报告生成脚本，产出一个排版清晰、可导航的 Word 文档：

```bash
python scripts/build_report.py 你的诊断数据.json 诊断报告.docx
```

生成的 docx 里包含：封面（项目名称、证据等级、评审标尺、日期）、通过性结论摘要、各分项表（每张表都带"专篇原文摘录"列）、A/B/C 缺陷清单（三个级别用不同颜色区分）。整份报告是**一个文件**，用 Word 直接打开即可。

## 三、修订稿与意见回复的复核规则（R1 到 R6）

复核"专家意见加修改回复加修订稿"这一组材料时，按下面六条执行：

- **R1 回应要具体**：每一条回应都要写清楚在哪一页、具体改了什么内容。只写"已在第 XX 页修改"却不说改了啥，视为没闭环。
- **R2 不许说空话**："已核实无误"这类回应必须附上核实的依据，否则退回。
- **R3 答要对题**：回应要和问题一一对应。问的是设备放大对比表，就不能拿产量放大表来交差。
- **R4 附件要实证**：图纸、附件类的修改必须写明图号或附件名，而且要实际调出来看，不能只凭回应的文字就采信。
- **R5 全文要联动**：改了某处参数或表述后，要全文检索这个关键词的所有出现位置，逐一同步核对。（有过真实教训：防爆等级在第 147 页改了，第 226 页还是旧的，被专家当场抓到。）
- **R6 改完要重扫**：每一轮修订完成后，数据一致性的闭环核验要全量重跑一遍，而不是只查改动的那几处。

## 四、专篇专有核查清单（9 条，是第 5、6、8 步的排错直觉）

> 下面 9 条是从真实案例里总结出来的高频缺陷模式。逐条执行，每一项都在诊断报告里单列一节呈现结果（就算没查出异常，也要留痕说明查过了）。

1. **跨文种的基础数据要互锁，但不能因此免检**：辨识类别、临界量取值、储量口径、名录比对这四项，要和安评同源同版本——但安评的错误会一位一位沿传过来。（真实案例：甲基肼的 J1/J2 两个值互相矛盾、乙腈临界量被放大了 115 倍、浓硫酸被系统性遗漏，这些都是从安评传进来、而专家没拦住的。）所以辨识类的计算必须独立重算。
2. **排查评审闭环里的失信点**：凡是回复"已核实、已补充"的点名项，都要到终稿里穿透核查。真实案例：意见要求"配平反应方程式"却没闭合（脱羧反应式的原子不守恒）；"物料平衡表"投入产出不平（差了 60.00 吨，投入侧没有硫酸、产出侧却有硫酸钠）；附件缺了联锁逻辑图和小试总结。图片形式的反应方程式要提取出来，用眼睛核配平。
3. **重大危险源辨识变更后要联动**：设计阶段储量或物料变了以后，"不属于重大变更"这个论断，前提必须是用更正后的辨识结论来判断；还要单独评估是否触发国家安监总局第 45 号令第十四条的重新评价情形。不能先下结论再凑论据。
4. **辨识表内部的规则**：排查同一物质有没有被双列双计、做算术复核、物料危险性在各表之间要能互证。（易燃液体的类别 3、乙类，和火灾危险性的甲 B 类，两者必有一处错，而这直接影响防火间距和防爆分区的基准。）
5. **储量的多个口径要互证**：原辅材料表、设计范围一节、辨识计算一节、一致性说明这四处的储量必须同源；依托的和新建的要分开标注；储罐物料在厂内的运输方式不能写成"叉车"。
6. **意见子项级的缺项**：一条复合意见配一段很长的回复时，容易掩盖掉单个设施的缺项。（真实案例：水封井漏了；仪表气的气源压力低报警联锁漏了——气动切断阀失气时往哪个方向动作，依赖气源的可靠性，这个联锁很关键。）所以复合意见要拆成可核查的子项，逐条核销。
7. **法定辨识清单要穿透核查**：易制毒（硫酸属于第三类，必查）、高毒物品（对照目录原文核）、监控化学品（名录表述的逻辑如果存疑要复核）。
8. **配伍与混存要有论证**：火灾危险性类别相差悬殊的物料放在同一个罐组、间距标准那一栏是"-"的时候，泄漏后混合的风险必须补充论证。（真实案例：甲 B 类的甲基肼和戊类的盐酸放在同一罐组，存在酸和肼混合的风险。）
9. **编辑质量核查**：依据过期（负面清单的版本、被替代的标准、名称和编号混搭）；表编号交叉引用错误；车间编号在不同地方写法不一；标准号有错字；单位缺失；设备表序号跳号或重复。

## 五、中试专篇的特有要点（涉及中试时加载）

- **小试总结、放大论证这两个附件**，是"已完成小试、工艺稳定"这个结论的唯一文件支撑。附件一旦缺失，结论就悬空了。
- 机械化换人、自动化减人的对照表，以及反应安全风险评估的采纳情况表，都是必备章节。
- 重点监管工艺的控制方案，要对照监管要求逐条列表，给出仪表位号、报警值、联锁动作。
- 依托在建或尚未验收的设施时，"验收合格后再投用"要上升为结论性的前置条件。
- 中试还会叠加属地的路径要求（比如山东的鲁化安转办〔2019〕49 号，实行备案登记制）：备案回执要附卷、不得在生产装置上做中试、中试产品不得销售、规模达到工业化量级时要论证它的"科研性"属性。

## 六、诊断红线（都是实战教训）

- **两个文种犯同样的错，不等于互相印证**：安评的错误会一位一位沿传进专篇，所以辨识类计算必须独立重算。
- **同一家机构的错误往往是"技术传统"**：发现某个设计或评价机构有某一类错误模式后，就按这个模式去排查，而不是当成个案。
- **识别空表**：模拟计算类的表格必须打开核对有没有数据行。（曾经出现过：表格只有表头，正文却写"由以上数据可知"。）
- **扫描模板残留**：凡是出现了本项目根本不存在的介质、装置名、设施，那就是套模板套错了、张冠李戴，相关结论一律无效。
- **先做数值的几何可行性检验**：储量、供气量这类数，先问一句"物理上可能吗"（用罐容乘密度乘充装系数乘台数，算出几何上限）再说。
- **穿透辨识里的分母和比值假设**：比值类指标，如果分母的假设异常，结论就失真了，要做敏感性重算。
- **对证据等级要有自省**：L3（没接规范库）情况下给出的任何"合标"或"超标"判断，都只是待复核的初判，不能作为终审否决的唯一依据；要显著提示补接规范库再复核。

## 七、参考文件索引（按需加载）

| 参考文件 | 用途 | 什么时候读 |
|---|---|---|
| [references/checklist-completeness.md](references/checklist-completeness.md) | 章节完整性要件表（AQ 3066-2025 和 39 号导则并轨，逐章列必备内容和三态判据） | 第 4 步 |
| [references/standard-anchors.md](references/standard-anchors.md) | 安全设施标准锚点库（按设施类别，含库内标准编号、检索关键词、高频错误、内置硬值） | 第 6 步 |
| [references/calc-cards.md](references/calc-cards.md) | 专篇计算复核公式卡（事故水、消防水、气体检测、氮气、储量、外部防护距离） | 第 7 步 |
| [references/regulation-index.md](references/regulation-index.md) | 规范库映射索引（150 本标准的编号、年份、路径、领域），加检索封装和时效核查表 | 第 3、6 步，检索时 |
| [references/output-templates.md](references/output-templates.md) | 输出模板（章节完整性表、设施符合性矩阵、计算复核表、意见闭环表、问题映射表、通过性结论） | 第 10 步 |
| [scripts/weknora_probe.sh](scripts/weknora_probe.sh) | 证据层探测和三层检索封装（`chem_probe`、`chem_search`、`wk_api`），source 后复用 | 第 3 步，检索时 |
| [scripts/build_report.py](scripts/build_report.py) | 把诊断数据 JSON 生成单个 Word 报告 | 第 10 步输出时 |
