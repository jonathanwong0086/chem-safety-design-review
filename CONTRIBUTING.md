# 贡献指南

感谢你对 `chem-safety-design-review` 的关注！本项目欢迎各类贡献。

## 贡献方式

### 1. 报告问题 (Issues)

发现以下情况时请创建 Issue:
- 标准依据过期或引用错误
- 诊断流程中的逻辑缺陷
- 文档说明不清或示例错误
- 真实专篇诊断中遇到的未覆盖场景

**Issue 模板**:
```markdown
**问题类型**: Bug / 标准更新 / 功能请求 / 文档改进

**涉及文件**: SKILL.md / references/xxx.md / scripts/xxx.sh

**问题描述**:
（清晰描述问题,必要时附截图或专篇脱敏片段）

**期望行为**:
（应该如何工作）

**环境信息**:
- 证据等级: L1 / L2 / L3
- 专篇编制依据: AQ 3066-2025 / 39号导则
- 项目类型: 新建 / 改扩建 / 中试
```

### 2. 提交代码 (Pull Requests)

#### 开发流程
1. Fork 本仓库
2. 创建特性分支 (`git checkout -b feature/your-feature-name`)
3. 提交改动 (`git commit -m 'feat: 描述你的改动'`)
4. 推送到分支 (`git push origin feature/your-feature-name`)
5. 创建 Pull Request

#### Commit 规范
遵循 [Conventional Commits](https://www.conventionalcommits.org/):
- `feat: 新增功能` (如新增计算卡/新增设施类别锚点)
- `fix: 修复问题` (如标准引用错误/公式纠正)
- `docs: 文档更新` (如 README/注释改进)
- `refactor: 重构` (如脚本封装优化)
- `chore: 杂项` (如 .gitignore 更新)

#### PR 检查清单
- [ ] 修改涉及标准依据时,已核对现行版本原文
- [ ] 新增锚点值已标注 `[原文核校]` 或 `[需复核]`
- [ ] 修改计算公式已附标准条款出处
- [ ] 更新 CHANGELOG.md (在 `[Unreleased]` 节)
- [ ] 通过真实专篇片段验证 (如适用)
- [ ] 文档中文表述清晰无歧义

### 3. 标准更新贡献

规范库索引 (`references/regulation-index.md`) 与锚点库 (`references/standard-anchors.md`) 需随标准更新维护:

**标准废止/替代时**:
1. 更新 `regulation-index.md` 对应标准版本年
2. 在 `M3 法规时效核查要点` 补充"需专门搜索复核"项
3. 核查该标准在 `standard-anchors.md` / `calc-cards.md` 的所有引用
4. 若参数/限值变动,更新锚点值并标注新版本

**新增标准覆盖时**:
1. 在 `regulation-index.md` 对应专业类别表追加标准条目
2. 若属安全设施类,在 `standard-anchors.md` 补充新类别或扩展现有类别
3. 若涉可计算项,在 `calc-cards.md` 补充公式卡

### 4. 基准案例贡献

真实专篇诊断中发现的**高频缺陷模式**或**颠覆性 A 类错误典型案例**,可脱敏后贡献:
- 在 Issue 中描述案例 (标注 `案例` 标签)
- 说明错误模式、发现路径、复核方法
- 附诊断报告片段 (脱敏)

维护者会评估后纳入 `专篇专有核查清单 (9条)` 或诊断红线,并致谢贡献者。

## 代码风格

### Bash 脚本
- 函数名用小写下划线分隔 (`chem_probe`, `wk_search`)
- 环境变量用大写下划线 (`EVIDENCE_LEVEL`, `CHEM_STD_LIB`)
- 用 `[ ]` 而非 `[[ ]]` (POSIX 兼容)
- 关键参数缺失时明确报错并 `return 1`

### Markdown 文档
- 中文与英文/数字间加空格 (如 `L1 检索` 而非 `L1检索`)
- 表格用管道对齐
- 公式/代码/标准条款用反引号标识
- 标准号首字母大写 (如 `GBT50493` 而非 `gbt50493`)

### 引用标准条款
- 明确标注标准号＋章节号 (如 `GBT50493-2019 第4.2.1条`)
- 从标准原文提取的值标 `[原文核校 标准号-版本年]`
- 未经核实的值标 `[需复核]`
- L3 内置锚点卡的值一律标 `[需复核]`

## 不接受的贡献

以下 PR 将被拒绝:
- ❌ 将 L3 内置锚点卡的 `[需复核]` 擅自去除 (除非提供标准原文截图证据)
- ❌ 降低 M1–M7 强制工序的执行标准 (如"可选择性执行")
- ❌ 将专篇专有核查清单的"必须"改为"建议" (基于实证教训总结,非可选)
- ❌ 添加与专篇评审无关的其他文种诊断逻辑 (本技能仅专注专篇单一文种)
- ❌ 未经讨论的大规模重构 (请先开 Issue 讨论技术方案)

## 行为准则

参与本项目即表示同意遵守以下准则:
- 尊重所有贡献者,保持专业与建设性讨论
- 对标准理解存在分歧时,以现行标准原文为准,而非个人经验
- 涉及争议性诊断判据时,引用标准条款/监管文件/司法解释等权威依据
- 不得泄露真实专篇中的敏感信息 (企业名/项目名/地理位置/工艺配方)

## 许可协议

贡献内容将按 [MIT License](LICENSE) 授权,你保留署名权但同意开源使用。

## 联系方式

- **Issues**: https://github.com/your-org/chem-safety-design-review/issues
- **Discussions**: https://github.com/your-org/chem-safety-design-review/discussions

---

再次感谢你的贡献！每一个标准更新、每一个案例补充、每一个逻辑改进,都让这个诊断技能更接近"政府专家上会审查水平"的目标。
